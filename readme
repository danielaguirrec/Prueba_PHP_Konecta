Para iniciar el programa, se cambian las credenciales ( nombre de base de datos, usuario, clave y host). Esto se hace en MODELO/conectar.php 
en la linea 8            $conexion = new PDO('mysql:host=localhost; dbname=inventario', 'root','');

luego comienza el programa en index.php

y ya
